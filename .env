# In all environments, the following files are loaded if they exist,
# the latter taking precedence over the former:
#
#  * .env                contains default values for the environment variables needed by the app
#  * .env.local          uncommitted file with local overrides
#  * .env.$APP_ENV       committed environment-specific defaults
#  * .env.$APP_ENV.local uncommitted environment-specific overrides
#
# Real environment variables win over .env files.
#
# DO NOT DEFINE PRODUCTION SECRETS IN THIS FILE NOR IN ANY OTHER COMMITTED FILES.
# https://symfony.com/doc/current/configuration/secrets.html
#
# Run "composer dump-env prod" to compile .env files for production use (requires symfony/flex >=1.2).
# https://symfony.com/doc/current/best_practices.html#use-environment-variables-for-infrastructure-configuration

###> symfony/framework-bundle ###
APP_ENV=dev
APP_SECRET=
APP_SHARE_DIR=var/share
###< symfony/framework-bundle ###

###> symfony/routing ###
# Configure how to generate URLs in non-HTTP contexts, such as CLI commands.
# See https://symfony.com/doc/current/routing.html#generating-urls-in-commands
DEFAULT_URI=http://localhost
###< symfony/routing ###

###> trailer/replicate ###
# 1 = only scene 1 uses real video/voice when Replicate is enabled; 0 = all scenes use real.
TRAILER_REAL_FOR_FIRST_SCENE_ONLY=1

# 1 = parallel scene pipelines (Spatie Fork, requires pcntl) and parallel voice+video when both are non-empty; 0 = sequential.
TRAILER_PARALLEL_FORK=1

# Replicate video provider (HTTP API) configuration.
# DO NOT put real tokens in committed files; override in .env.local or real env.
TRAILER_VIDEO_REPLICATE_ENABLED=0
TRAILER_VIDEO_REPLICATE_API_TOKEN=
# Example: model or version identifier as required by Replicate
TRAILER_VIDEO_REPLICATE_MODEL=
# Optional default preset key when calls omit replicate_preset (see ReplicateVideoModelPresets)
TRAILER_VIDEO_REPLICATE_DEFAULT_PRESET=
# Polling interval (seconds) between status checks for long-running predictions
TRAILER_VIDEO_REPLICATE_POLL_INTERVAL_SECONDS=5
# Maximum number of polling attempts before giving up
TRAILER_VIDEO_REPLICATE_MAX_ATTEMPTS=60
# Wall-clock poll cap in seconds (0 = only max_attempts applies)
TRAILER_VIDEO_REPLICATE_POLL_TIMEOUT_SECONDS=0

# Replicate voice (MiniMax Speech 2.6 Turbo). Uses TRAILER_VIDEO_REPLICATE_API_TOKEN when enabled.
# voice_id must be a valid MiniMax catalogue voice (see Replicate model API tab); placeholders like TON_VOICE_ID are rejected at startup.
TRAILER_VOICE_REPLICATE_ENABLED=0
TRAILER_VOICE_REPLICATE_MODEL=minimax/speech-2.6-turbo
TRAILER_VOICE_REPLICATE_VOICE_ID=
TRAILER_VOICE_REPLICATE_AUDIO_FORMAT=mp3
###< trailer/replicate ###
